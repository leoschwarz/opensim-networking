// TODO: Stuff like this should probably be combined in a nice place, instead
// of cluttering the root of the networking crate.

/*
use nalgebra::{Vector2, Vector3};
use std::fmt::Debug;

#[derive(Clone, Debug, PartialEq)]
pub struct GlobalPos2<T: 'static + Copy + Debug + PartialEq> {
    p: Vector2<T>,
}

#[derive(Clone, Debug, PartialEq)]
pub struct LocalPos2<T: 'static + Copy + Debug + PartialEq> {
    p: Vector2<T>,
}

#[derive(Clone, Debug, PartialEq)]
pub struct GlobalPos3<T: 'static + Copy + Debug + PartialEq> {
    p: Vector3<T>,
}

#[derive(Clone, Debug, PartialEq)]
pub struct LocalPos3<T: 'static + Copy + Debug + PartialEq> {
    p: Vector3<T>,
}

// TODO: Include RegionHandle here? (Or is this really only relevant for the grid map?)
*/
